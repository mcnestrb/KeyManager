<% if @groups.count == 0 %>
    <p>You are in no groups</p>
<% else %>
    <ul>
    <% @groups.each do |group| %>
        <li><%= link_to group.name, group_path(group) %></li>
    <% end %>
    </ul>
<% end %>
